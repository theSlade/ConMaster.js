```js
// Import the package
const { passGen } = require("conmaster.js");

const passGen = passGen.passGen;

// PassGen(6), that mean it will generate a password from 6 characters.
console.log(passGen(6));
```
